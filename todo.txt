todo:
take care of name changes by modifying the sheet
clean data somehow b/c this: https://osu.ppy.sh/community/matches/45442499

data cleaning:
add the changed names to teams sheet then rerun
use google scripts to do it after every link is in. 

make it so that it reads off a google sheet